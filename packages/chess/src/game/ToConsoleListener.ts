
export default {
  /*
   private _console: Console = {
    writeln: (m?: string, type_?: string): void => {
      if (m) {
        console.log(`[${type_ ? type_ : ''}]${m}`)
      }
      else {
        console.log('[]:\n')
      }
    }
  }


        let squareString = ''
      let commaFirst = false
      squareInCheckFrom.forEach((s) => { 
        if (commaFirst) {
          squareString += ', ' 
        }
        else {
          commaFirst = true
        }
        squareString += positionToString(s)
      })
      this._console.writeln(`Check! ${side} in check from ${squareString}!`, 'check')

  */
}